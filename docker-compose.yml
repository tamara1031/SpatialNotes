version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
    ports:
      - "8080:8080"
      - "4321:4321"
    environment:
      - NODE_ENV=development
    tty: true
    stdin_open: true
    command: make dev
